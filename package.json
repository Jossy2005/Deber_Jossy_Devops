{
  "name": "deber_josselyn-mena",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
  "test": "jest",
  "build": "mkdir -p package && cp -r src package/"
  },

  "devDependencies": {
    "jest": "^29.0.0"
  }
}
